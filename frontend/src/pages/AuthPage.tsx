import{useState,type FormEvent}from'react';import{BriefcaseBusiness,CheckCircle2}from'lucide-react';import{useAuth}from'../auth';
export default function AuthPage(){const{login,register}=useAuth();const[mode,setMode]=useState<'login'|'register'>('login');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{if(mode==='login')await login(String(f.get('email')),String(f.get('password')));else await register({name:f.get('name'),email:f.get('email'),password:f.get('password'),course:f.get('course'),graduationYear:Number(f.get('graduationYear'))})}catch(x){setError((x as Error).message)}finally{setBusy(false)}}return <div className="auth"><section className="auth-art"><div><span className="eyebrow">PLACEMENT, SIMPLIFIED</span><h1>Move from campus<br/>to career.</h1><p>One clear place to discover opportunities, submit applications, and follow every step of your hiring journey.</p><ul><li><CheckCircle2/>Curated placement opportunities</li><li><CheckCircle2/>Transparent application tracking</li><li><CheckCircle2/>Actionable placement analytics</li></ul></div></section><section className="auth-form"><form onSubmit={submit}><div className="brand"><span className="logo"><BriefcaseBusiness/></span>PlaceFlow</div><h2>{mode==='login'?'Welcome back':'Create student account'}</h2><p className="muted">{mode==='login'?'Enter your details to continue.':'Start managing your placement journey.'}</p>{mode==='register'&&<><label>Full name<input name="name" required/></label><label>Course<input name="course" placeholder="Computer Science"/></label><label>Graduation year<input name="graduationYear" type="number" min="2024" max="2040" required/></label></>}<label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button><button className="link" type="button" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'New student? Create an account':'Already registered? Sign in'}</button>{mode==='login'&&<div className="demo">Demo: student@placeflow.dev / Student@123</div>}</form></section></div>}
